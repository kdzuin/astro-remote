name: Web Client Tests

on:
  push:
    branches: ["main"]
    paths: ["src/webclient/**"]
  pull_request:
    paths: ["src/webclient/**"]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/webclient
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Run unit tests
        run: node --test

      - name: Verify committed build output (tailwind.css + sw.js) is up to date
        run: |
          npm ci
          npm run build
          if ! git diff --quiet -- tailwind.css sw.js; then
            echo "Build output is stale — run 'npm run build' and commit the result." >&2
            git diff -- tailwind.css sw.js >&2
            exit 1
          fi
